spec:
  template:
    spec:
      containers:
        - readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5


